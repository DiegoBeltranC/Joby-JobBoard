import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true, // Forzar recarga dev server - Filtro de CVs activos
};

export default nextConfig;
